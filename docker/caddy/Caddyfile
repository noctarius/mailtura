:80 {
    # Proxy all /api requests to backend
    handle_path /api/* {
        reverse_proxy mailtura:3000
   }

    # All other requests go to frontend
    handle {
        reverse_proxy frontend:5173
    }
}